# Release asset provenance

This record covers the static public-site media prepared for the Desk Setup
Switcher v0.1.0 public-beta candidate. The assets are derived from checked-in
DEBUG-only synthetic UI fixtures. No installed personal profile, live Capture,
Apply, permission request, UI automation, or display/audio/network mutation was
used to create them.

The Capture and Edit source screenshots were recorded by commit
`39dc972a7359b75f46aef9f6e38577039f852220` (`Complete P2 UI refinement`).
The Review source was regenerated from the current tree on 2026-07-18 after its
rollback sentence was corrected to promise an attempt rather than a guarantee.
All fixture models use deterministic synthetic identities, an isolated
temporary profile store, no system snapshot adapters, and an empty apply
registry. The current Review generator's focused test passed one case in one
suite; its three raw PNGs were visually inspected and then stripped of host ICC
and ancillary metadata before being retained under
`docs/evidence/public-release-assets/workflow/`. The corresponding AX records
reject concrete `/Users/` paths and password text. These properties establish
the data-source boundary; they do not prove installed-window behavior, real
hardware support, Apply, or rollback.

## Asset manifest

| Public asset | Source and transformation | SHA-256 | Synthetic and verification boundary |
| --- | --- | --- | --- |
| `site/public/app-icon.svg` | Exact copy of `Assets/AppIcon.svg`; original project artwork | `c183b584887cd946d0d0a4d3b1da77749ef4016ea6886c7ef798b7339aa1d109` | Contains no third-party logo or device artwork. The verifier requires byte identity with the canonical SVG. |
| `site/public/screenshots/capture.png` | `docs/evidence/p2-ui-refinement-2026-07-17/tray/01-empty-en-light.png` (`1c27cd5cc417b9dfa78075dbec2c0b6f567390ad27fa84305ebe28724be5bf94`), mechanically normalized with an equivalent FFmpeg white-background alpha flatten, RGB24 conversion, and `-map_metadata -1` | `e0e7467a5c647590213a10f2dc5dcdd70624231255833c4a132c75ff69ffc02e` | English light empty state, `368×260`. Shows the Capture entry point only; Capture was not invoked. Public derivative is opaque RGB with no embedded ICC, textual, or host-identifying metadata; FFmpeg retains a benign unitless `pHYs` chunk. |
| `site/public/screenshots/edit.png` | `docs/evidence/p2-ui-refinement-2026-07-17/settings/13-display-en-light.png` (`629a8c110b8385869d7d3ba4a0e1242a97d78736a911ff80bf45065d52be642d`), mechanically normalized to RGB24 with source-identifying metadata and the ICC profile removed | `bb1573547e6e56e2d92072a7848463f575c90d13c2a80e59cf90d7984e3a9c5b` | English light synthetic Display editor, `900×568`. The footer explicitly identifies synthetic UI-audit mode; no save or hardware catalog operation occurred. Public derivative is opaque RGB with no embedded ICC, textual, or host-identifying metadata; FFmpeg retains a benign unitless `pHYs` chunk. |
| `site/public/screenshots/review.png` | `docs/evidence/public-release-assets/workflow/23-apply-preview-en-initial.png` (`f729dfb87d61e502a6fe5ec41b439f76a516a31e55d99ff8e9354df6aaec6d71`), generated by `TrayOffscreenEvidenceTests.rendersApplyPreviewStates`, stripped of ancillary metadata, then normalized by `scripts/build-public-demo.sh` with a white-background alpha flatten, RGB24 conversion, and `-map_metadata -1` | `8f8a83b8784d194c95490f4f54a47910fbdb4d565971ef2a460e8390d73122dd` | English light initial normal Apply Preview, `620×500`. The warning says the app will **attempt** restoration. Planned values are hard-coded test data and the fixture confirmation closure performs no action. Apply and rollback were not run. Public derivative is opaque RGB with no embedded ICC, textual, or host-identifying metadata; FFmpeg retains a benign unitless `pHYs` chunk. |
| `site/public/demo/desk-setup-switcher.mp4` | The three normalized public screenshots, composed by `scripts/build-public-demo.sh` in Capture → Edit → Review order with two 0.6-second FFmpeg fades; H.264, `1280×720`, `yuv420p`, 37 seconds, input metadata excluded | `bdf80f4e52eec3509df8bdb0cfd07192089ec764f2a64188ec6ac3b1f226b7e0` | 458,239-byte silent synthetic product tour. It does not animate a click, claim successful Apply, or simulate hardware effects. The Review frame remains visible at the end. Standard MP4 brand, handler, and FFmpeg encoder tags remain; no source path, device, network, or personal metadata is present. |
| `site/public/demo/captions.en.vtt` | Original English caption copy written for the 37-second synthetic tour | `467bb3f0c453709fd38398b3ea9a7ef7451abb872734be6e691215c2309a27b5` | Five contiguous WebVTT cues from `00:00.000` through `00:37.000`; calls the artifact a public-beta candidate and keeps Apply a separate confirmation. |
| `site/public/demo/captions.ko.vtt` | Original Korean caption copy written for the same 37-second synthetic tour | `c43c6be5cc894cb4ea6de48695a0382b22b991b24896def62a99bffe427cc1b9` | Five contiguous WebVTT cues matching the English timing and verification boundary. |
| `site/public/og.png` | `docs/evidence/public-release-assets/og-background-imagegen.png` (`ee29d142b55020ca65fd7196ed3bb2c8a861111bab94ffe30fd3b2a330b6f543`), exact public icon, normalized Edit screenshot, and evergreen English copy composed at `1280×640` by `scripts/build-social-preview.swift`, then normalized to opaque RGB24 with ancillary metadata removed | `b3e8b8a8ec5141d4a5e4486702d9f48a09e9ce0ccfb073bbffa88ddca69627a2` | The abstract background was produced with OpenAI image generation and contains no person, place, device, logo, or factual scene. The final composition was visually inspected, is 553,956 bytes, and has no embedded profile. |

The public PNG normalization was performed with FFmpeg 8.1.2. The original
Capture/Edit shell history was not retained, so those two rows record the
equivalent operation rather than claiming a byte-for-byte command log. The
current Review and video are deterministic outputs of
`scripts/build-public-demo.sh`. The resulting public files are authoritative:
`sips` reports their exact dimensions, `hasAlpha: no`, RGB storage, and
`profile: <nil>`. The three FFmpeg-produced public screenshots retain only a
benign unitless PNG `pHYs` ancillary chunk in addition to image data; the MP4
retains normal container/codec tags. Neither is represented as zero metadata.

## Social-preview background prompt

The built-in OpenAI image-generation tool was called once for the background
layer. The generated bitmap was inspected before use, then copied into the
repository under the hash in the manifest. Product UI, iconography, and text
were not generated; the final card composites the exact public project assets
and deterministic AppKit typography over this background.

```text
Use case: ads-marketing
Asset type: background layer for a 1280×640 GitHub social preview card
Primary request: Create a refined, understated background for the open-source macOS utility “Desk Setup Switcher”.
Scene/backdrop: edge-to-edge deep navy-to-evergreen gradient with a very subtle blue-teal glow and restrained translucent frosted-glass arcs; no horizon or room.
Composition/framing: exact 2:1 landscape composition. Keep the left 55% calm and dark for later typography. Make the right 45% slightly brighter as a stage where an actual application screenshot will be composited later. Preserve generous safe margins.
Style/medium: premium native macOS launch artwork, crisp, minimal, quiet, trustworthy, not futuristic.
Color palette: midnight navy, charcoal, muted evergreen, restrained cyan-blue highlights.
Constraints: background layer only; no text, no letters, no UI, no app windows, no icons, no logos, no devices, no people, no mockups, no decorative particles, no watermark. Do not imitate or include third-party brands.
```

## Licensing

The app icon, fixture data, caption copy, composition, and original interface
artwork are project work distributed under the MIT License. The abstract social
preview background is an OpenAI image-generation output commissioned by the
project. Under the applicable [OpenAI Terms of Use](https://openai.com/policies/terms-of-use/),
the requester owns the output as between the requester and OpenAI to the extent
permitted by law; the project distributes its rights in that output under MIT.
The generated source and its hash are retained so the origin is not represented
as human-made. Product screenshots and the derived video include SF Symbols
only in context as part of the macOS interface. They do not redistribute
individual symbols; Apple's applicable platform terms continue to govern those
embedded symbols. No third-party logo, stock artwork, or personal device
screenshot is included.

## Reproduction and verification

The release media must remain pinned to the source and derivative hashes above.
`site/public/assets.sha256` pins all eight public derivatives, while
`docs/evidence/public-release-assets/sources.sha256` pins the two historical
fixture sources, generated background, and three current PNG/AX evidence pairs.
After regenerating the current synthetic Review fixtures with the explicitly
gated test environment, strip their host metadata with
`scripts/strip-png-metadata.swift`, retain the three PNG/AX pairs under the
recorded workflow directory, then run:

```sh
scripts/build-public-demo.sh
scripts/build-social-preview.sh
make verify-public-assets
```

The verifier requires `ffmpeg`, `ffprobe`, `sips`, `shasum`, and standard macOS
command-line tools. Missing media prerequisites are failures, not skipped
checks. It verifies exact PNG geometry, opacity and absence of embedded ICC
profiles; scans embedded strings for high-confidence credential, personal-path,
private-network, and device-profile patterns; verifies the video format,
duration and absence of audio; and validates both caption timelines. Both
checksum manifests are mandatory, must contain exactly their declared files,
and are checked from their respective base directories.

This automated evidence does not OCR image pixels or certify complete
accessibility. Before publication, a maintainer must visually compare each
public derivative with its named source fixture, review the English and Korean
copy, and record the final social-preview source and checksum only after that
file actually exists.
