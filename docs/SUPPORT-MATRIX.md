# Support matrix

## Legend

- **Implemented:** source path exists, but no verification level is implied.
- **Unit verified:** deterministic pure logic passed without constructing live system adapters.
- **Mock verified:** deterministic injected/unit coverage passed; the real host was not changed.
- **Live-read verified:** an opt-in read-only test passed on the named hardware.
- **Hardware-mutation verified:** an explicit interactive apply and rollback procedure passed on physical hardware.
- **Experimental:** implementation uses a public preferences API with an undocumented key or lacks a stable supported OS contract.
- **Unsupported:** intentionally omitted; the app reports an item-level reason.
- **Pending:** implementation or required evidence is incomplete.

“Unit verified,” “mock verified,” and “live-read verified” must never be shortened to “hardware verified.” As of 2026-07-22, **no live setting mutation is hardware verified**. The current `v0.0.9`/build-1 source passed 5,116 non-live deterministic checks/assertions in `make verify`: 516 app checks (192 XCTest + 323 default Swift Testing cases across 39 suites + one isolated native popover case in a 40th Swift Testing suite) plus 4,600 release-tooling assertions (374 base release-policy + 8 Mach-O compatibility verifier + 659 remote-controls v1 policy/normalizer + 210 remote-controls v2 lifecycle-policy + 86 remote-controls v3 lifecycle-policy + 71 remote-controls v2 collector + 17 remote-controls v3 collector + 407 publication-approval policy + 1,216 external-beta/inventory/lineage/template policy + 132 collector-wrapper mock + 19 release-evidence history + 57 draft-reconciler mock + 261 artifact-restoration mock + 545 approved-publication mock + 306 legacy-workflow-containment mock + 232 shell/workflow guard assertions). All build/Analyze stages and unsigned development-package verification passed. The current bilingual site, guides, exact-commit synthetic media, 39-assertion audit fixture suite, and complete-history/current-asset production scan passed their local gates on 2026-07-22. The DMG was not installed, launched, signed for distribution, uploaded, or published. The inventory/lineage and external-beta results are synthetic contract tests, not actual history review or tester evidence. The earlier P2 pass retains its separate 461-check and 39-fixture/79-artifact evidence. Comprehensive assistive-technology certification is not claimed or required for the public-beta gate; keyboard behavior, accessibility names/values, and non-color cues remain source/test requirements. A separate 2026-07-20 opt-in read-only baseline passed all 506 then-current app cases, including Display, Audio, Network, Input, ConditionContext, and ApplyLivePreparation group/base paths, on Apple Silicon/macOS 26.5.2 without setting mutation. It did not itemize actual ColorSync-profile, input-volume, or exact service-IPv4 field presence/read on that host, so those item-level claims and all apply/rollback paths remain mock-only; TCC, login-at-boot, live Keychain write, and setting mutation also remain unproved.

## Tray Surface v2 evidence

| Contract | Automated/offscreen evidence | Manual status |
| --- | --- | --- |
| One status item, popover, and hosting controller | Factory/count/behavior tests verify one `.applicationDefined` popover, one existing `NSHostingController`, and one public parent content controller. The isolated genuine `NSPopover.show(...)` regression preserves AppKit's nonzero wrapper origin while keeping the hosted child centered in zero-origin local bounds | The reinstalled package completed 20 measured popover opens. Arrow/material/outside-click behavior and focused-control AX observation were not separately completed |
| Empty-state horizontal layout and CTA | The structural boundary still converts native `T3/L11/B5/R2` to hosted `T3/L0/B5/R0`, preserving content at `x16…352/y21…297`. P2 placement policy adds one labelled body-level Capture CTA only for pristine empty/idle state and keeps one compact header action everywhere else | Preceding 20-open measurements reported exact center delta `0`; the historical P2 DMG reinstall and bounded changed-surface inspection passed. Capture itself was not invoked |
| Fixed open-session viewport | Deterministic 0/1/3/10-profile, small-screen, scale, screen-change, state-change, and reopen tests pass | Installed multi-monitor/scale transition pending |
| Action disposition and handoff | Every action is mapped; stay-open cannot close; visible/key order, bounded non-key liveness, independently cancellable coalesced callers, red-close, same-turn immediate reopen, stale completion, failure, and quit-only termination pass. Settings routing now synchronously selects Profiles before every presentation and stale completion owns no later tab write | Preceding installed Settings/workflow geometry and Escape close passed; P2 installed `⌘,` routing passed from visible System/About and close→reopen/stale cases. Full native focus-transfer timing, keyboard traversal, and focused-control AX observation remain pending |
| App-lifetime state/workflows | Dismiss/reopen task survival, deletion focus, persistent-window ownership, synthetic permission suppression, minimum-size adaptive actions, safe keyboard focus policy, and accessibility focus intent pass. App-owned disclosures expose explicit localized state/hint and remove collapsed children | Installed Return expands and Space collapses with AX value/hint/child presence updated. VoiceOver was not run; full VoiceOver certification is excluded and unclaimed rather than a pending beta gate |
| Catalog parity and bilingual-path rendering | The [P2 audit](P2-UI-REFINEMENT-AUDIT-2026-07-17.md) and [P2 evidence index](evidence/p2-ui-refinement-2026-07-17/README.md) record 39 fixtures and 79 generated and inspected artifacts: 15 tray, 13 Settings, three apply-preview, two responsive-workflow, and six auxiliary fixtures, plus one Settings footer comparison note | Native linguistic review, real text size/contrast/transparency, and complete focused-control AX observation remain pending and nonblocking for P2. Full-app VoiceOver certification is excluded and unclaimed |

## Platform

| Area | Target | Current evidence |
| --- | --- | --- |
| macOS | Planned 14 Sonoma or later | Deployment target is 14.0, but the current host evidence is macOS 26.5.2. At least one exact-candidate clean lifecycle report on Apple Silicon/macOS 14 Sonoma remains mandatory before macOS 14 support is claimed |
| Apple Silicon | arm64 | The planned initial public-beta target is Apple Silicon only. Default tests, the current universal development package, and the 2026-07-20 source-baseline opt-in group/base read-only paths passed on an Apple M5 Mac running macOS 26.5.2 without mutation |
| Intel Mac | x86_64 | Current Swift/Xcode Debug/Release and development package contain an x86_64 convenience slice, but no physical Intel execution evidence exists. Intel is not part of initial public-beta support and the slice must not be reported as supported hardware |
| Distribution | Contributor-only unsigned/ad-hoc DMG; protected Developer ID/notarized DMG required for public beta | Current `v0.0.9`/build-1 development evidence is `artifacts/Desk-Setup-Switcher-0.0.9-unsigned.dmg`, SHA-256 `92f4b5af619465c8b953b63d8cc770cacadc839f338ab724a7a2591c498b9e19`. Checksum, mount, metadata/resources, exact `arm64+x86_64`, per-slice macOS 14.0 `LC_BUILD_VERSION`, and ad-hoc/no-Developer-ID classification passed; it was not installed, launched, uploaded, or published. The P2 package/install hashes remain historical below |
| CI | GitHub Actions | UI-hardening commit `5f0cabc` passed full `make verify` and unsigned-package upload in [run `29181900967`](https://github.com/GGULBAE/desk-setup-switcher/actions/runs/29181900967). Initial failure run `29154880831` and repair run `29155207923` remain historical Swift 6.1 compatibility evidence |
| App Store | Not required | No sandbox/App Store claim |
| Signing/notarization and publication | Local fail-closed implementation present; protected credentialed run pending | The proposed manual workflows and release-only scripts require exact tag/commit confirmation, an ephemeral signing Keychain, Developer ID + hardened runtime + timestamp, signed DMG, accepted notarization, stapling, app-and-DMG Gatekeeper checks, checksum/SBOM/attestations, exact draft redownload, approval-bound one-PATCH publication, and exact public redownload verification. These paths passed simulated policy/guard/mock tests but are unpushed, unconfigured, and have not run with real credentials; the current app remains ad-hoc signed development evidence only |

## App, profiles, and safety engine

| Area | Capability | Status and evidence |
| --- | --- | --- |
| App | Background/menu-bar-only lifecycle | `LSUIElement` and the app-owned `NSStatusItem`/`NSPopover` source retain the preceding build/package verification. Persistent Settings/workflow presentation has a liveness deadline, consumer-local cancellation, red-close detachment, immediate-reopen defense, and identity-guarded stale completion. P2 final-DMG reinstall and bounded routing/disclosure inspection passed; full focus and accessibility behavior remain pending |
| App | Login item | Launch at login is now off by default and registers only after explicit user opt-in. Deterministic tests cover fresh state, opt-in registration, preserving consented on/off choices, stale enabled-state cleanup, and resetting/removing legacy prerelease automatic registration. Approval-required/retry and actual login-at-boot after a reboot remain pending; the historical default-on installed evidence below is not current behavior |
| App | English/Korean UI | Integrated lint passed key parity, duplicate-key, placeholder, and statically discoverable-key checks. Exact runtime localization tests cover both bundles. The dated [2026-07-14 audit](MANUAL-UI-AUDIT-2026-07-14.md) visually reviewed six English and six Korean states, including long strings and 680×480 layout; later P2 synthetic evidence is recorded separately, and complete current interactive and linguistic-quality review remains pending |
| App | Accessibility | Current source contains icon labels/help, non-color state and validation text, deterministic deletion focus, adaptive workflow actions, safe Cancel/Revert/Close keyboard targets, heading/status accessibility focus intent, shortcuts, one combined remaining-time value, and explicit app-owned disclosure value/hint/keyboard semantics. The 39-fixture/79-artifact review passed, and installed Return/Space activation updates AX value/hint/child presence. VoiceOver was not run and no VoiceOver claim is made. These results do not prove full-app speech/rotor/focus, keyboard-only first-responder rings, contrast, transparency, or real macOS text-size behavior |
| App | System, About, and diagnostics presentation | Profiles/System/About remains the primary navigation, and every Settings/`⌘,` route synchronously selects Profiles without allowing stale completion to restore another tab. Advanced Diagnostics now has a 520×360 minimum and 640×460 ideal contained by the 680×480 Settings minimum. Korean accessibility-text fixtures cover direct About, diagnostics, and Settings/About; final-DMG `⌘,` routing passed from visible System/About and close→reopen/stale cases. Diagnostics clearing remains unrun |
| Profiles | CRUD, ordering, selection, metadata, per-option inclusion | Mock verified at the storage/domain boundary. **New Profile** is direct; Duplicate/Delete, Move Up/Down, and Import/Export are grouped in one secondary menu. Legacy false activation normalizes to active applicability and never filters a row or blocks apply. Tray deletion confirmation remains inline; Cancel/`Esc` leaves storage untouched |
| Profiles | Saved/draft editing, replacement protection, and metadata merge | Deterministic draft, dirty-Apply, and app-integration cases verify save/discard/cancel before planning, save-failure preservation, and latest stored-profile use. The installed same-window check preserves a valid unsaved draft, selection, Save availability, and field focus across 980→680→980 |
| Profiles | Current-settings capture | Tray Capture creates and selects a new reviewable profile. A pristine empty/idle tray now presents exactly one labelled body CTA; nonempty and non-idle states present exactly one compact header action. The removed draft-refresh action cannot replace an open draft. Synthetic/installed inspection does not invoke Capture |
| Profiles | Flat typed setting-value editor | Display/Audio/Network remain always expanded. Every leaf says **Apply with profile**, shows Included/Not included text and a non-color symbol, and exposes a setting-specific accessibility label/value/hint. `VisibleSettingRegistry` still limits the surface to nine complete setting kinds; unavailable saved values retain an inclusion-off repair control. Thirteen Settings fixtures and 27 artifacts were inspected; full option-by-option installed keyboard and focused-control AX observation remain nonblocking follow-ups. Full VoiceOver certification is excluded and unclaimed |
| Profiles | Value-first summaries, icon preservation, and technical disclosures | Unit policy covers explicit disclosure Expanded/Collapsed value, next-action hint, state ownership, and collapsed-child absence. Attached/offscreen review passed; installed Return expands and Space collapses with AX value/hint/child presence updated. VoiceOver was not run and is not claimed. Full profile-row click-through remains pending |
| Profiles | Versioned JSON, schema 0→1 migration, semantic/resource limits | Mock verified. Missing, Boolean, floating, fractional, and future schema versions fail closed; explicit integer schema 0 remains the only migration source. Included Display ColorSync without a value is rejected before planning |
| Profiles | Atomic primary/backup, corruption quarantine/recovery, local file permissions | Temporary-directory tests verify normalized app-store paths, one-leaf/non-reserved file names, traversal rejection, 0700 directories, 0600 managed/staged files, extended-ACL removal and absence verification, no-follow descriptor-bound reads, regular-file/owner checks, pre/post-open identity and mutation detection, last-known-good recovery, identity-bound permission repair, identity-preserving quarantine/restore, exclusive quarantine rename, and file sync before commit. Managed staging, missing-destination exclusive rename, existing-destination atomic swap, guarded rollback/cleanup, and directory sync stay relative to one verified parent-directory descriptor. Deterministic tests replace the parent path and regular/symlink destination leaves, exercise both post-commit rollback branches, and verify that redirected or displaced objects are not followed or deleted. Darwin has no public inode-conditional rename/unlink, so a noncooperative same-UID process retains a final syscall-adjacent leaf race. Abrupt interruption between an existing-leaf swap and displaced-leaf unlink can leave the former private `0600` file under its randomized staging name; parent sync remains best effort and does not justify a perfect sudden-power-loss guarantee |
| Profiles | Generic storage-error recovery | Generic failures use a prominent error heading/icon/card instead of only a footer caption, disable the profile workspace while global recovery owns the surface, and clear stale editor feedback. Initial load offers one Retry Loading action; ordinary operation failure offers one Dismiss Error action; editor-owned save failure suppresses a duplicate global error. Temporary-store tests cover workspace/error ownership, action ownership, repeated failed retry, and sanitized unsafe-file failures |
| Profiles | Import/export | Import uses descriptor-bound snapshot reads and rejects path traversal, symlinks, non-regular files, wrong owners, oversize content, source replacement/in-place mutation, invalid schema/semantics, and duplicate IDs before committing. Export reads only the persisted document when a draft is dirty, writes and synchronizes a short ACL-free `0600` staging file relative to one identity-bound user-selected parent descriptor, and uses `RENAME_EXCL` so an existing entry wins without cooperative replacement. The parent must be current-user-owned without shared POSIX writes, current-user-owned and sticky, or root-owned and sticky such as macOS `/tmp`; other-owner, non-sticky shared-write, and any ALLOW-ACL parent fails closed while deny-only ACLs remain compatible. Deterministic success, ACL rejection/removal, destination-race, existing regular/symlink/FIFO/directory, parent/staging replacement, maximum-leaf, pre/post-commit, mode, and cleanup cases pass. The normal path publishes only complete bytes, but a noncooperative same-UID process retains the documented adjacent-syscall race; abrupt termination or detected staging replacement can leave a staging entry for inspection, and parent-directory sync remains best effort. Interactive file-panel/import/export review remains pending |
| Snapshot | Detected/storable/unreadable/permission-required/unsupported internal classification | Internal classification remains mock verified. Current-source Display, Audio, Network, and Input group/base paths were live-read verified on one Apple Silicon Mac running macOS 26.5.2; the run did not itemize actual ColorSync-profile, input-volume, or exact service-IPv4 field presence/read, so those item-level claims remain mock-only. Current user-facing capture results include saved applicable leaves and actionable permission-required items only; snapshot-only, unreadable, and unsupported evidence remains available to sanitized diagnostics but does not turn capture into a noisy partial-result card. The network adapter no longer reads or emits service order. Runtime display/audio/network identifiers remain absent from retained presentation state |
| Snapshot | Capability-group integrity | A group mismatch fails closed: no mismatched snapshot payload is ingested, the requested group is normalized to unsupported/unreadable, and unrelated groups continue. Deterministic unit coverage exists; this is not hardware evidence |
| Snapshot | Dormant applicability normalization | Full-gate unit/mock verified and idempotent for capture, decode, local store, import, and planning. Legacy activation is forced true; hidden Input, display origin/rotation/active, system-output/mute, Wi‑Fi power/SSID, global IPv4, DNS, and proxy values remain stored but excluded; group inclusion derives from visible leaves. All cases use synthetic values |
| Readiness | Ready/Partial/Unavailable manual plan state | Existing adapter/readiness behavior is mock verified, and 2026-07-20 source-baseline ConditionContext plus ApplyLivePreparation group/base read-only paths passed on Apple Silicon/macOS 26.5.2 without mutation. Current manual readiness ignores dormant legacy conditions, uses normalized applicability, and no longer lets historical applied/failed result state permanently mask a refreshed calculation; deterministic/app-source coverage is present |
| Readiness | Legacy condition compatibility | Core all/any/inverted evaluation remains unit/mock verified and data remains round-trip compatible. Condition editing is absent, and existing/imported conditions are deliberately dormant and non-blocking for current manual readiness, preview, and Apply. Historical choice/input-validation tests are compatibility evidence, not a current UI feature |
| Apply | Normal/force preview, omissions, zero-operation rejection, no-op filtering | Mock verified in both modes. Audio, ColorSync, and service-IPv4 regressions distinguish an already-satisfied no-op from an unavailable changed target; changed unavailable targets are explicit omissions rather than silent success. The fresh snapshot profile produced a zero-operation plan with both actions disabled and no mutation |
| Apply | State-aware Review action and explicit confirmation | Ready + operations opens Review; Partial + executable operations opens Review Available in the same slot. The preview states that nothing changed, and only Apply Profile starts execution. A text-and-shield Beta status says Apply and rollback are not hardware-verified and asks the user to check System Settings afterward; it does not rely on color or add another decision. Review content and decision actions share one scroll sequence, so the 520×360 accessibility-text fixture must pass the plan before reaching Apply; Escape remains cancel, and Apply has no default Return shortcut. Full target names remain available to accessibility. A stable reviewed plan reaches its adapter once; a changed preflight returns to a marked refreshed review with zero adapter calls. No current installed click-through or hardware execution evidence |
| Apply | Dirty-draft and pre-execution stale-plan defense | Current source resolves same/other dirty drafts before preparing, then reloads profile/system state and execution-relevant rollback payloads; save failure/cancel cannot proceed. JSON object order is canonicalized while changed values, rollback payloads, and non-JSON bytes remain blocking. Pure regressions and a selected-profile adjacent read-only normal/available-items check pass; no live execution |
| Apply | Immediate result and post-apply read-back | Full-gate unit/mock verified as a compact result card plus itemized detail with success/failure/skipped/unsupported/rollback/not-verified counts. After execution or display Keep, fresh planning marks a succeeded operation that remains required—or cannot be read because capability, snapshot, fatal validation, or infrastructure preparation is unavailable—as not verified while keeping intentional force omissions separate. Rollbacks reconcile by operation UUID, so same-key operations are not conflated and successful rollback does not erase an initiating failure. This is mock/read-back evidence, not hardware mutation proof |
| Apply | One active transaction, deterministic order, fatal reverse rollback | Mock verified, including rollback failure separation |
| Apply | 15-second high-risk confirmation and protected rollback token | Mock verified with temporary apply, confirmation commit/failure, timeout, revert, and rollback. User-facing copy now says the app will **attempt** to restore prior settings, matching the best-effort adapter/transaction boundary; real display mutation was not run |
| Diagnostics | Redaction and rotating local JSONL files | Mock verified for secrets, exact location, SSID, IP host portions, home paths, permissions, rotation, concurrency, and clearing |
| Diagnostics | Browse/refresh/clear from System | Implemented with sanitized events under Application Support and presented as optional troubleshooting. The advanced sheet now fits within the Settings minimum and stacks long accessibility-text rows/actions. P2 diagnostics fixtures are included in the inspected 39-fixture/79-artifact manifest; refresh/clear and real persisted-event flows remain pending |

## Open-source public-beta baseline recorded on 2026-07-22

| Release surface | Current evidence | Publication boundary |
| --- | --- | --- |
| Local verification | On 2026-07-22, `make verify` passed 5,116 deterministic checks/assertions against the current `v0.0.9`/build-1 source: 516 app checks (192 XCTest, 323 default Swift Testing cases across 39 suites, and one isolated native popover case in a 40th Swift Testing suite) plus 4,600 release-tooling assertions (374 base release-policy, 8 Mach-O compatibility-verifier, 659 remote-controls v1 policy/normalizer, 210 remote-controls v2 lifecycle-policy, 86 remote-controls v3 lifecycle-policy, 71 remote-controls v2 collector, 17 remote-controls v3 collector, 407 publication-approval policy, 1,216 external-beta/inventory/lineage/template policy, 132 collector-wrapper mock, 19 release-evidence history, 57 draft-reconciler mock, 261 artifact-restoration mock, 545 approved-publication mock, 306 legacy-workflow-containment mock, and 232 shell/workflow guard assertions). Lint/localization policy, Swift Debug/Release, universal Xcode Debug/Release, Analyze, package/checksum, mounted resources, exact `arm64+x86_64` slices, per-slice macOS 14.0 `LC_BUILD_VERSION`, and ad-hoc/no-Developer-ID classification passed | This is non-live development evidence, not a real inventory review, external beta run, credentialed signing/notarization run, hardware verification, protected-remote proof, or public-release approval |
| Development package | Current `v0.0.9`/build-1 unsigned/ad-hoc universal `artifacts/Desk-Setup-Switcher-0.0.9-unsigned.dmg`, SHA-256 `92f4b5af619465c8b953b63d8cc770cacadc839f338ab724a7a2591c498b9e19` | It was not installed, launched, signed with Developer ID, notarized, stapled, uploaded, or published |
| Public repository audit | On 2026-07-22, the 39-assertion fixture suite and production scanner passed against the current complete, non-shallow Git history and tracked worktree image/assets. General credential, personal-path, image-metadata, and Git-identity checks cover the worktree and reachable history; the rejected-template release-evidence subscan also checks stage-0 index bytes independently. Replacement refs and grafts fail closed; annotated-tag traversal starts at every ref and follows nested tag objects | Passing this scoped audit is not a guarantee that no sensitive material exists and does not OCR historical pixels; protected-branch/environment settings, private vulnerability reporting, and final maintainer review remain operator work |
| Remote release controls | A read-only [remote-controls audit](REMOTE-RELEASE-CONTROLS-AUDIT-2026-07-18.md), rechecked through authenticated GET/list operations on 2026-07-20, 2026-07-21, and 2026-07-22, recorded the following at 2026-07-22 13:48 KST: remote `master` was `1489b7fcb41ffa7e55a43ed65e6befc538838140`; the audited local release-control source `de4f805cfadf5fdfd1266d07286d27192794ca33` was its descendant by 39 commits. Remote workflows were only CI plus active Release workflow `311269012`; branch protection returned 404/`protected:false`, and rulesets, environments, repository Actions secret/variable names, `v*` tags, Releases, and Release-workflow runs were zero. Immutable Releases, private reporting, and SHA pinning were disabled; Actions were unrestricted; public metadata/support gaps were present. Public/MIT/Issues, the read-only workflow token, secret scanning, push protection, Dependabot security updates, and existing successful remote-master CI were enabled. The private uncommitted mode-0600 receipt `legacy-workflow-containment-plan-de4f805-20260722.json`, under a mode-0700 parent, bound that exact local HEAD/four tool blobs and active/zero-tag/zero-Release state at plan digest `bb53444e29871dd077d975128f0d41299d484d694429939a5e9f7daa11e0d4e6`; a separate `validate-plan` invocation using the same checked-in validator passed. The v3 lifecycle collector separately binds all four intended workflow blobs, both CI jobs, two protected environments, actor/credential-name/manual-record identity, and the three phase boundaries. | No apply, PUT, push, tag, dispatch, or other remote mutation occurred. The private receipt is planning evidence rather than containment and is accepted only from a clean checkout of its bound HEAD; a later HEAD must create a new plan. At the observation, the historical workflow was active remotely, while the safer workflow and inert legacy tombstone were local and unpushed. The authoritative policy is intentionally `configured:false`, so live lifecycle verification stops before resolving `gh`. The old workflow must be disabled under explicit approval, protections configured, the reviewed branch merged through passing CI, all three phase gates passed at their exact lifecycle points, and the separate Settings/manual controls reviewed before publication |
| Release signing identity inventory | The synthetic import lifecycle requires exactly one valid codesigning identity in the ephemeral Keychain and an exact match to the reviewed Developer ID Application label; extra-valid, missing-match, and malformed-count inventories fail closed and remove the temporary signing directory | This is deterministic mock evidence only. No real certificate, Developer ID identity, or credentialed signing run was used |

| Approval-bound publication | A closed-schema approval validator and exact-ID publisher restore and byte-verify both attempt-1 origins, prove the linear predecessor/final first-parent evidence histories and both direct annotated tags, then fully reverify the predecessor with its tag's own code in a private detached worktree. It also verifies fresh remote-controls/Settings evidence, actor and token owner, approval chronology, the exact final draft/nine assets/attestations, both named approval-commit CI jobs with distinct integer identities, and immutable-release state immediately before permitting one exact `draft:false`, `prerelease:true`, `make_latest:false` PATCH, then re-downloads the public Release and all final assets. | The workflow is unpushed, unconfigured, and unrun. Any `HUP`, `INT`, `QUIT`, `TERM`, cancellation, host loss, incomplete log, or ordinary failure after a PATCH attempt is explicitly incident-only, not retry evidence. No Release was changed or published |
| Local public surface | User-first README, English/Korean guides, schema/adapter references, launch copy, bilingual static site, and three synthetic screenshots regenerated from exact committed source `bc3ec58e2f1f13e3c47be3e879fed957564012cd` pass `make verify-public-surface`. The rebuilt silent bilingual-captioned demo and deterministic social preview are pinned by eight-file public and 11-file source/AX manifests; public PNGs are opaque, profile-free, and critical-chunk-only, and unexpected source-evidence files now fail verification | The tracked closed-schema launch state remains `holding` with no download URL. Project code adds no analytics, client tracking, project-set cookies, account, database, object storage, service binding, or download. Hosting-provider operations and two pinned tab-scoped router guards are disclosed. The CI change is unpushed; deployment, clean-link verification, and final manual browser/copy review remain pending |
| Planned beta hardware | Apple Silicon with a macOS 14.0 deployment target | Current runtime evidence is Apple Silicon/macOS 26.5.2. One of the three exact-candidate external lifecycle reports must pass on Apple Silicon/macOS 14 Sonoma before that support claim is made. The x86_64 convenience slice is cross-built but physically unverified; Intel is not initially supported |
| Accessibility boundary | Localizable English/Korean copy, keyboard behavior, accessibility names/values, and non-color cues remain required | Full VoiceOver certification is explicitly excluded and is not a publication gate or a claimed capability |
| Canonical release pipeline | Both exact protected origins require independent Developer ID, hardened runtime, timestamp, signed-DMG, accepted-notarization, staple/validation, app-and-DMG Gatekeeper, checksum, SBOM, and provenance/attestation verification. Only the final origin proceeds through draft/public redownload | Protected environments/secrets, remote repository settings, private vulnerability reporting, canonical site/release publication, three external clean-install beta reports with the mandatory predecessor upgrade, and explicit publication approval remain pending |

## Non-live UI-hardening evidence recorded on 2026-07-12

The historical header/editor follow-up passed full local `make verify` with 215 tests: 112 XCTest cases with five opt-in skips plus 103 Swift Testing cases with one opt-in skip. Across both frameworks, five were read-only hardware cases and one was a Keychain-write case. The 56 presentation-specific cases comprised 29 draft XCTest cases and 27 presentation/condition Swift Testing cases. Localization lint covered English/Korean key parity, duplicate keys, format placeholders, and statically discoverable localized UI keys. Swift and universal Xcode Debug/Release, Analyze, package, checksum, mounted resources/architectures, and ad-hoc signature classification passed.

No live flag was set. This evidence includes no screenshot walkthrough, full keyboard or VoiceOver run, TCC grant/deny action, login-item state mutation, hardware read, Keychain write, setting apply, or rollback. The then-current local DMG SHA-256 was `45772d20e6d7655c41ed4ff5d0261257b98f1361f4cf8cc38ebf837720d5820b`. UI-hardening commit `5f0cabc` passed [Actions run `29181900967`](https://github.com/GGULBAE/desk-setup-switcher/actions/runs/29181900967), but that CI artifact predates the local header/editor follow-up.

## Apply-reliability evidence verified on 2026-07-14

Integrated non-live `make verify` passed lint/localization policy; 290 default cases (124 XCTest with five opt-in skips plus 166 Swift Testing cases with one opt-in skip), zero failures; Swift Debug/Release; universal Xcode Debug/Release; Analyze; package/checksum; mounted metadata/resources/`x86_64 arm64`; and ad-hoc/no-Developer-ID signature classification. Across both frameworks, five skips are read-only hardware cases and one is a Keychain-write case. Separately, `git diff --check` passed. Deterministic synthetic cases cover state-aware normal/available-items selection, cached refresh and duplicate preparation, dirty-Apply decisions, complete/partial/failure capture summaries, read-back unavailable/not-verified result classification, UUID-based rollback reconciliation, field-specific validation, unsupported applicability normalization and idempotence, storage/import boundaries, adapter capture defaults, and immediate input-preference read-back disagreement. Existing mock transaction tests continue to cover normal/force planning, omissions, stale-plan defense, fatal reverse rollback, and display safety.

The verified artifact is `artifacts/Desk-Setup-Switcher-0.1.0-unsigned.dmg`, SHA-256 `417ffbb20b6a77b9037f42d5acb998574460374675e746715474e17f9f772615`. It is build/mount verified only; it was not installed, quarantined, launched, or mutated against hardware. No push or new CI run was performed.

This historical apply-reliability evidence is unit/mock/read-back only. At that milestone it did not include a live Apply, actual display/audio/network/input mutation, hardware rollback, a new live read, UI automation, full VoiceOver, TCC grant/deny, Keychain write, physical Intel execution, Developer ID signing, notarization, or release publication.

## Final-source synthetic and installed UI audit on 2026-07-15

The DEBUG-only host used synthetic fixtures, a temporary store, empty system/apply dependencies, disabled permission requests, and public-action suppression verified by `UIAuditSafetyTests`. The [audit report](MANUAL-UI-AUDIT-2026-07-14.md) records 16 rendered PNGs and 16 read-only AX logs from the Xcode Debug app bundle. All were visually/structurally inspected and privacy scanned. The four 2026-07-15 additions show the current complete bilingual inline deletion/capture feedback and regular/minimum editor feedback states. A separately authorized installed interaction used `DESK_SETUP_LIVE_UI_TESTS=1`, a complete preflight backup, and byte-for-byte rollback; it changed no system setting.

| Requested state | Source/test evidence | Remaining boundary |
| --- | --- | --- |
| Ready normal Apply | `ApplyWorkflowPresentationTests` verifies the normal primary action | No current-tree click-through |
| Partial Apply Available with operation plus omission | `ApplyWorkflowPresentationTests` verifies force preview keeps the display operation and separates the DNS omission | No live execution |
| Unavailable and no-op | `ApplyWorkflowPresentationTests` verifies distinct disabled reasons; overview evidence includes disabled and no-op rows | No current v2 popover interaction |
| Dirty draft Save/Discard/Cancel and Apply | `DirtyApplyProtectionTests` verifies same/other-profile targets, save success/failure, cancel, and latest draft use | No manual dialog/keyboard walkthrough |
| Actionable capture permission result | Current `ProfileCaptureSummaryBuilderTests` prove snapshot-only, unreadable, unsupported, and runtime-identifier evidence is omitted while a permission-hidden Wi-Fi name remains value-free and actionable. Injected app tests cover authorization-request and System Settings-open routing without live TCC actions, including the invariant that the persistent app System tab is requested before delayed system UI. The runtime coordinate reader/cache and Settings refresh surface are removed; Location authorization is used only for the current Wi-Fi SSID during explicit Capture. The scoped installed interaction showed the privacy explanation and a persistent app System window at 100 ms and 600 ms after Continue | macOS resolved Location as already allowed, so the exact permission-result-card action and full denied/granted TCC matrix remain pending. No coordinate request/storage is part of the supported runtime |
| Inline validation | Tests verify field identifiers and validation rules; English/Korean screenshots and AX logs expose non-color summary, first issue, and reveal action | Focus movement after activating the action is pending |
| Success/partial/failure/rollback/not-verified results | `ApplyWorkflowPresentationTests` verifies counts, UUID rollback reconciliation, unavailable read-back, and profile readiness mapping. P2 adds a 520×360 long Korean failed/rollback accessibility-text fixture with scrollable body and stable Close action | The fixture was generated and inspected; installed Apply/result-sheet execution was outside scope and no Apply was invoked |
| Tray deletion | Exact-ID request/cancel/confirm, fixed viewport, focus recovery, and normal/confirmation overflow are deterministic-test verified; current English/Korean detached-host evidence shows complete actions | Installed v2 Esc, Cancel, Confirm, and full keyboard traversal remain pending. Historical `MenuBarExtra` interaction is not reused |
| Stable Settings workspace | Policy and owned-window geometry tests cover one 210-point sidebar + 390-point-minimum editor anatomy, resizable style, 680×480 minimum, fixed footer/action bars, at-most-one refresh adoption per presentation, no reset when invoking an already-visible window, and stable window ownership. P2 adds synchronous Profiles routing and a contained diagnostics sheet | Preceding installed `500×300`→`680×480` clamping and ten `900×568` opens pass; P2 `⌘,` routing passed from visible System/About and close→reopen/stale cases; full content-state, flicker, keyboard, and focused-control AX review remains manual |
| Flat visible groups and options | Source contains no group/option disclosure state. Registry and projection tests verify only complete supported controls render; standard/minimum/large-text evidence covers Display, Audio, Network, ColorSync, and DHCP/manual forms | Full installed option-by-option keyboard and focused-control AX walkthrough remain pending; full VoiceOver certification is excluded and unclaimed |
| English/Korean and long strings | Catalog lint and exact runtime-bundle tests cover both languages; P2 adds 520×360 and 680×480 Korean accessibility-text fixtures for the changed auxiliary surfaces | The final 39-fixture/79-artifact review passed; interactive linguistic-quality audit remains pending |
| Keyboard and accessibility structure | Localized AX policies cover labels, values, descriptions, validation metadata, de-duplicated profile rows, and explicit disclosure state/hint | Installed Return expands and Space collapses with AX value/hint/child presence updated. Full keyboard-only order and focused-control AX observation remain nonblocking follow-ups. VoiceOver was not run; full VoiceOver certification is excluded and unclaimed |

## System capabilities

| Group | Capability | API/mechanism | Status and evidence |
| --- | --- | --- | --- |
| Display | Active display discovery and stable identity | Public Core Graphics | Mock verified and 2026-07-20 source-baseline group/base path live-read verified on one Apple M5 Mac running macOS 26.5.2. The gate preserves count/identity/mode checks when active and verifies a typed nonfatal empty snapshot when the online session display is asleep |
| Display | Primary display and extended/mirrored topology | Core Graphics display configuration | Current visible slice is mock verified; live mutation not run |
| Display | Mirroring | Core Graphics display configuration | Mock verified; live mutation not run |
| Display | Logical/pixel mode and refresh rate | Core Graphics display modes | Mock verified; live mutation not run. Read-only snapshots now carry typed ephemeral supported-mode catalog entries for the editor picker; this catalog is runtime context and is not persisted in profile JSON |
| Display | Portable ColorSync ICC profile selection | Public ColorSync | Current capture/edit/validate/plan/apply/verify/rollback slice is mock verified. The 2026-07-20 source-baseline Display group/base read-only path passed, but it did not itemize actual ColorSync-profile presence/read on that host; item-level live read and all mutation/rollback remain unverified |
| Display | Temporary apply, confirmed commit, and rollback | Core Graphics app/session configure scopes | Mock verified: apply is app-only, **Keep Changes** re-commits session-only, and rollback is session-only; live mutation/timeout/app-exit behavior not run |
| Display | x/y origin | Persisted snapshot compatibility only | Dormant and excluded from the current surface/planner; no mutation claim |
| Display | Rotation mutation | No safe public implementation | Unsupported; rotation is snapshot-only |
| Display | Activation/deactivation mutation | No safe public implementation | Unsupported; active state is snapshot-only |
| Audio | Device/UID/scope/default/control discovery | Public Core Audio | Mock verified and 2026-07-20 source-baseline group/base path live-read verified on one Apple M5 Mac running macOS 26.5.2 |
| Audio | Default input/output | Public Core Audio properties | Current visible slice is mock verified; live mutation not run. System-output UID remains persisted compatibility data but is dormant in the default surface |
| Audio | Input/output scalar volume | Device-scoped capability/settable Core Audio properties | Current visible slice is mock verified for current and profile-selected target UIDs; unsupported changed controls become item omissions while already-matching values remain no-ops. The 2026-07-20 source-baseline Audio group/base read-only path passed, but it did not itemize actual input-volume field presence/read on that host. Mute remains dormant; item-level live read and all mutation/rollback remain unverified |
| Audio | Microphone capture | Not used | Unsupported and not requested |
| Network | Interfaces, link, IP/subnet, gateway, DNS, services, and legacy global IPv4/proxy snapshot | CoreWLAN, SystemConfiguration, getifaddrs | Mock verified and 2026-07-20 source-baseline group/base path live-read verified on one Apple M5 Mac running macOS 26.5.2; service order is absent rather than snapshotted |
| Network | Exact Ethernet/Wi-Fi service DHCP/manual IPv4 | Authorization Services plus public SystemConfiguration preferences | Current capture/edit/validate/plan/apply/verify/rollback slice is mock verified, including exact serialized rollback, dynamic-store completion, and authorization/failure taxonomy. The 2026-07-20 source-baseline Network group/base read-only path passed, but it did not itemize actual exact service-IPv4 field presence/read on that host; no authorization prompt, service write, or hardware rollback was run |
| Network | Wi-Fi power | CoreWLAN | Retained but dormant in the current surface; mock verified; live mutation not run |
| Network | Saved-network association | CoreWLAN/macOS-held saved-network credential | Retained but dormant in the current surface. Mock verified: target saved profile/access is preflighted and current association must have a safe rollback target; no password enters profiles/operations/logs; live mutation not run |
| Network | SSID read | CoreWLAN with authorization-aware result | Retained compatibility data but dormant in the current surface. Powered-on nil SSID is treated as ambiguous/unavailable, never proof of safe disassociation; mock verified and the 2026-07-20 source-baseline Network group/base read-only path passed, but the denied/granted value matrix is pending |
| Network | Global IPv4, DNS, and proxy mutation | No current public rollback-safe surface | Retained compatibility values may be snapshotted but remain dormant and are omitted from current planning. Exact per-service Ethernet/Wi-Fi DHCP/manual IPv4 is the separate authorized path above; service order is absent rather than persisted |
| Input | Pointer speed and natural scrolling snapshot/apply | CFPreferences, undocumented global keys | Experimental and dormant in the current surface; mock verified and 2026-07-20 source-baseline group/base read-only path passed on Apple Silicon/macOS 26.5.2; live mutation not run |
| Input | Key repeat, repeat delay, standard F-key snapshot/apply | CFPreferences, undocumented global keys | Experimental and dormant in the current surface; mock verified and 2026-07-20 source-baseline group/base read-only path passed on Apple Silicon/macOS 26.5.2; live mutation not run |
| Input | Immediate write read-back | CFPreferences snapshot after synchronize | Deterministic injected tests require the freshly read value to match; disagreement returns failure instead of success. This is immediate adapter read-back/mock evidence, not proof of perceived hardware behavior |
| Conditions | Display, audio input/output, USB/hardware, SSID, Ethernet, IP/CIDR | Core evaluator plus public read-only discovery | Dormant in the current manual surface; compatibility behavior is mock verified, and the 2026-07-20 source-baseline ConditionContext group/base read-only path passed on Apple Silicon/macOS 26.5.2 |
| Conditions | Authorized recent location | Dormant profile-schema compatibility only | Legacy evaluator semantics remain mock verified, but the app no longer requests, caches, refreshes, or supplies coordinates at runtime. Imported location conditions are preserved but do not gate current manual readiness, preview, or Apply |
| Secrets | `SecretStore` generic-password implementation | Security framework, device-only after-first-unlock accessibility | Mock verified with synthetic bytes; live Keychain write deliberately not run |

## Explicitly unsupported features

- Automatic or background profile switching
- Logitech Options+, Razer Synapse, or other vendor profile control
- Vendor-specific DPI/button mapping or keyboard firmware settings
- Direct editing of Karabiner-Elements or any third-party app configuration
- UI scripting of System Settings or third-party apps
- Private macOS APIs, arbitrary shell execution, application telemetry, or cloud services
- Administrative network mutation without a public authorization and rollback design

## Read-only evidence

The historical 2026-07-11 local `make verify` gate passed with 158 tests: 83 XCTest and 75 Swift Testing cases. On 2026-07-20, 2026-07-20 source-baseline `DESK_SETUP_LIVE_READ_TESTS=1 make test` passed all 506 app cases on an Apple M5 Mac running macOS 26.5.2. The opt-in Display, Audio, Network, Input, ConditionContext, and ApplyLivePreparation group/base paths ran read-only, including the online-but-inactive display sleep state; the separate live Keychain-write flag was not enabled, and no setting mutation ran. These tests do not itemize actual ColorSync-profile, input-volume, or exact service-IPv4 field presence/read on this host, so those item-level claims and every apply/rollback path remain mock-only. No personal device identifier, SSID, IP address, or location is recorded in the repository.

## Historical local package evidence recorded on 2026-07-11

- Universal Debug/Release builds and the packaged executable contain `arm64 x86_64`.
- The post-fix no-Developer-ID DMG checksum is `246af7c21ac9f1ffd4c6f7523f857737f148e4354a948b0e4d9a2123bb5d827f`; mounted layout, bundle metadata, icon, English/Korean resources, and ad-hoc app signature passed the checked-in verifier.
- Downloaded CI artifact ID `8249295840` verified its checksum file and CI-generated DMG SHA-256 `d3894d8e7efdd775c5983c63051ec4181d33e039a40b83163a39a24c898be6b5`; it differs from the local checksum because the DMGs are not byte-for-byte reproducible.
- The recorded local-DMG fresh copy to `/Applications` launched background-only/menu-bar-only; the popover and Settings rendered in Korean, and an accessibility label passed inspection. This is baseline evidence, not a current UI-tree walkthrough.
- The fresh install created one schema-v1 Ready profile from a read-only snapshot with all four groups. Its zero-operation plan kept Apply and Force Apply disabled.
- Default-on `SMAppService` registration succeeded and BTM reported `[enabled, allowed, notified]`; UI opt-out disabled it, re-enable restored enabled status, and final cleanup opted out with only disabled BTM history.
- The local copy did not exercise quarantine/Gatekeeper, approval-required/retry states, or actual login-at-boot after a reboot. No full VoiceOver/keyboard, import/export, permission, or live-mutation procedure was run.

Coverage still absent:

- An external display, mirrored pair, display mode change, and unplug/replug identity matching
- An audio device without software volume/mute and a device hot-plug sequence
- Ethernet and both Wi-Fi permission-denied and permission-granted cases
- A physical Intel Mac
- Login-item approval-required/retry paths and actual login-at-boot after a reboot
- A downloaded/quarantined Gatekeeper install
- Any live setting apply or rollback
- A live Keychain round trip
- Full installed keyboard/focused-control AX, real text size, contrast, and transparency review. A full VoiceOver certification remains optional, non-gating, unrun, and unclaimed.

## Historical P2 and preceding structural evidence recorded through 2026-07-17

The [P2 UI refinement audit](P2-UI-REFINEMENT-AUDIT-2026-07-17.md) is the historical UI evidence boundary. Its final integrated count is 461, DMG SHA-256 is `342d804d8bbff51209af4bccefb405ee76499050c1e640a011d41e2f78792031`, and the reinstalled ad-hoc-signed executable SHA-256 is `fb35352fb6a9588c0c50269975ccd3d7b73e52010de10de132bf45d60236f719`. The app launched on Apple Silicon, and the installed executable contained `x86_64 arm64`; the x86_64 slice was not run. Profile primary/backup/defaults remained unchanged. The final DMG reinstall, `⌘,` routing, and installed disclosure keyboard/AX checks passed without Apply/Capture/TCC/login/hardware mutation. VoiceOver process and preference were both 0; VoiceOver was not run and is not claimed. The preceding audit retains the 446-check structural gate and historical package/install hashes.

| Capability | Implementation boundary | Preceding structural evidence |
| --- | --- | --- |
| Tray reopen geometry and anchor | 368-point width; 260/260/316/560-point 0/1/2/3+ tiers; static non-scrolling empty body; symmetric 16-point content inset; generation-keyed top scroll; one internal overflow scroll view; status-item width frozen while open | Deterministic tests use a title-sized anchor spy, asymmetric native safe area, deliberately displaced geometry, stale completion rejection, two-profile bottom gap, and late first layout. The reinstalled package completed 20 measured opens with exact center delta `0`; popover material/outside-click and full accessibility behavior remain manual |
| Apply-preview/workflow layout | Persistent 620×500 workflow window with 520×360 minimum, child screens fill one canvas, request-ID top reset, scrollable body above adaptive measured actions, responsive grid/stack rows, scrollable protected summaries, and monospaced combined countdown | Attached/offscreen fixtures pass. Installed request `500×300` clamped to `520×392`; the initial frame was `620×532` with `620×500` content, and Escape closed the window. Full keyboard/focused-control AX order and any hardware-changing confirmation remain manual; full VoiceOver certification is excluded and unclaimed |
| Persistent Settings/Profile Edit | App-lifetime controllers; red-close detaches then cancels the current producer; visible/key/front completion precedes tray close; shared `.regular`-while-visible/`.accessory`-when-hidden coordinator | Deterministic lifecycle coverage passes. Installed request `500×300` clamped to `680×480`, and normal `900×568` geometry was retained across ten opens. Full native focus ordering and focused-control AX observation remain manual |
| Settings profile actions | Fixed Revert/Save footer; no current-settings draft refresh action; tray Capture creates the current-state profile | Source policy and refreshed five-fixture detached evidence cover the simplified action bar. Save/revert behavior remains deterministic-test verified; corrected native rendering is manual |
| Status-item profile indicator | Fresh Ready plus known zero-operation match only; applying state wins; invalid/deleted/stale values fall back; legacy activation is ignored | Unit/AppKit tests cover fallback symbol, long name, preferred selected match, applying, failure, deletion, and a legacy false profile. A live status-item label was not inspected |
| Audio defaults and volumes | Public Core Audio UID catalogs; per-device volume catalog; default switch precedes target-device volume; scalar/percent conversion; exact read-back/rollback; non-settable controls absent as new choices but recoverable through an Include-off warning when a saved included change cannot run | Deterministic adapter, target-capability, lifecycle, repair-policy, and vertical-slice tests pass; supported and unsupported-volume screenshots are recorded. No live audio mutation or hardware rollback was run |
| Display topology and ColorSync ICC | Public Core Graphics app-only topology plus portable ColorSync ID/ICC SHA-256 target, current-catalog URL resolution, exact mapping read-back, and prior-mapping rollback; topology before color | Enumeration, stale/hot-plug, mismatch, order, rollback, and JSON-without-runtime-URL tests pass. ColorSync screenshots are recorded. No live display/color mutation or rollback was run; HDR/pixel encoding/vendor modes remain absent |
| Ethernet/Wi‑Fi service IPv4 | Portable kind/name/interface identity; exact serialized rollback catalog; authorized `SCPreferences` lock/set/commit/apply/unlock; dynamic-store completion; exact read-back; network-last apply and network-first protected rollback | Deterministic DHCP/manual, ambiguity/missing, authorization/failure taxonomy, early notification, zero-timeout, end-to-end apply/verify/rollback, and safety-order tests pass. Four Ethernet/Wi‑Fi DHCP/manual screenshots are recorded. No authorization prompt, service write, or hardware rollback was run |
| Simplified editor projection | Alias/symbol and flat Display/Audio/Network only; nine visible setting kinds must satisfy all seven vertical stages; legacy description/activation/conditions/Input and hidden leaves round-trip dormant; unused condition-editor app source removed | Registry, normalizer, real-adapter synthetic invariant, lifecycle, localization, accessibility-hint, and detached editor pairs cover standard, 680×480 minimum, accessibility large text, Korean, light/dark, and supported/unsupported projection. The [settings lifecycle refactor audit](SETTINGS-LIFECYCLE-REFACTOR-2026-07-16.md) records the current evidence boundary. Native SwiftUI localization context and complete linguistic review remain manual |

### Bounded P2 UI refinement

| P2 item | Current source/deterministic state | Remaining evidence boundary |
| --- | --- | --- |
| Empty-tray Capture | One labelled body CTA in pristine empty/idle state; one compact header action in every other state; exactly-one-action policy | Final-DMG bounded installed inspection passed; Capture was not invoked |
| Profile action hierarchy and Export | New Profile direct; three secondary menu groups; explicit saved-only dirty notice; persisted-document export regression | File-panel/import/export interaction remains pending |
| Inclusion meaning | Apply-with-profile title, Included/Not included text, non-color symbol, setting-specific accessibility value/help | Full option-by-option installed traversal remains pending |
| Disclosure | App-owned state/value/hint, keyboard button, collapsed-child absence | Installed Return/Space and AX state updates passed. VoiceOver was not run and no VoiceOver/full-app claim is made |
| Diagnostics/About/workflows | Diagnostics 520×360 minimum and 640×460 ideal fit Settings 680×480; Korean accessibility-text fixtures cover About, safety, result, error, diagnostics | Included in the inspected 39-fixture/79-artifact manifest; final-DMG bounded installed routing/disclosure inspection passed |
| `⌘,` routing | Synchronous Profiles route with visible, close→reopen, and stale-completion coverage | 461-check gate and bounded installed visible System/About, close→reopen, and stale checks passed |
| Storage failures | Prominent one-action Retry/Dismiss card; editor-owned failure is not duplicated | 461-check gate passed |

This bounded pass must not be reported as a completed full-app VoiceOver, rotor, keyboard-order, or focused-control AX walkthrough. Profile-store/import and login-item consent hardening were outside that P2 pass and are recorded in the later open-source baseline above; Apply/Capture execution, TCC, Gatekeeper, physical Intel, diagnostics clearing, and hardware mutation remain unverified.

For any later audio hardware procedure, test input and output volume separately and only when the adapter reports the corresponding scope settable. For network review, first confirm ambiguous/unresolvable services are absent, then use the exact DHCP/manual preflight and rollback steps below. Color verification means an enumerated ICC profile mapping only; do not infer HDR, pixel encoding, or vendor mode support.

## Manual hardware verification procedure

Every record must include date, macOS/app commit, broad hardware class with serials redacted, expected/actual result, rollback result, sanitized diagnostic event IDs, and tester. Never record a real SSID, exact location, IP host address, password, serial number, or raw exported profile.

### Shared preflight for every mutation

1. Obtain explicit approval for the named mutation and use a local build from a clean verified commit. Never run the procedure in CI.
2. Connect only the devices under test. Capture a read-only “Before” profile and independently record the original state in System Settings without sensitive values.
3. Confirm a manual recovery route: System Settings for audio/network; a second usable display or Screen Sharing for display tests; and an alternate working interface plus locally recorded original DHCP/manual state for network tests.
4. Apply only one setting group and the smallest reversible change. Use the app's preview; cancel if the plan contains an unexpected group, missing rollback data, or an ambiguous identity.
5. Verify the changed setting through the relevant macOS UI and a fresh read-only snapshot. Use **Revert Now** in the protected-change prompt; separately test timeout, red-close, and quit restoration. After **Keep Changes**, apply the “Before” profile and verify the original state independently.
6. Redact the resulting evidence and update this matrix. A successful apply without a successful restore is a failed verification.

### Display procedure

Use a built-in display plus one external display where available. Test primary/mirroring/mode separately, then choose a different ICC profile from the app's enumerated ColorSync list. Keep an independent screen/control path available. Verify the initial change is temporary/app-only; let the 15-second timer expire once and confirm restoration, test **Revert Now**, red-close, and quit, then repeat and choose **Keep Changes**. Finally restore the “Before” topology and ICC mapping. Do not test origin, rotation, activation, HDR, pixel encoding, or vendor modes; they are absent.

### Audio procedure

Use a device identified by Core Audio UID. Change one default role, then input or output volume only if the adapter projects that control as settable. Verify independently in Control Center/System Settings and restore. If a device has no software volume, verify that the editor omits only that control without failing unrelated audio settings. System output and mute are dormant and must not be tested from the default surface.

### Network procedure

Ensure an alternate working interface exists. Test Ethernet DHCP, Ethernet manual, Wi‑Fi DHCP, and Wi‑Fi manual separately on a non-production service, using synthetic/redacted evidence and no real host address in the repository. Confirm an ambiguous or missing portable identity is absent before apply. For each change verify authorization denial is nonfatal to unrelated groups, read-back matches, timeout/Revert/red-close/quit restores exact protocol data, and Keep followed by the “Before” profile restores independently. Wi‑Fi power/association, global IPv4, DNS, and proxies are dormant and must not be tested from the default surface; service order is not part of the profile/current surface.

### Input procedure

Input preferences remain backward-compatible experimental code but have no default editor/apply surface. Do not run input mutation as part of the current milestone.

### Keychain and platform procedure

Run the gated synthetic Keychain round trip only after explicit approval, then confirm the test item was deleted. It is separate from setting mutation evidence. Repeat the safe build, default tests, app launch, and read-only discovery on physical Intel hardware before changing the Intel row from cross-build only.
